Copy and paste the following link into your browser's address bar to confirm your email address:
&nbsp;
= en_verification_url(@user.token)
&nbsp;
(This needs to be done manually simply because this email is displayed in plain-text instead of HTML)
